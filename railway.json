{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pnpm --filter signal-dashboard dev",
    "restartPolicyType": "ON_FAILURE",
    "watchPatterns": [
      "apps/dashboard/**",
      "pnpm-lock.yaml"
    ]
  }
}
