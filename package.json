{
  "name": "colosseum-frontier",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npm run dev --prefix apps/dashboard & npm run dev --prefix apps/bot",
    "build": "npm run build --prefix apps/dashboard",
    "start": "npm run start --prefix apps/dashboard"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "npm@10.9.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "pm2": "^6.0.14",
    "lucide-react": "^0.454.0"
  }
}
