{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pnpm --filter signal-bot start",
    "restartPolicyType": "ON_FAILURE",
    "watchPatterns": [
      "apps/bot/**",
      "pnpm-lock.yaml"
    ]
  }
}
